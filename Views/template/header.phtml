<?php
// Only start a session when one doesn't exist yet
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
// Connect the database model to fetch user details
require_once(__DIR__ . '/../../Models/Database.php')
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport"
          content="width=device-width, initial-scale=1">
    <meta name="description"
          content="Pet Watch lets users report and view missing pets,
  helping owners to find their lost pets.">
    <meta name="author"
          content="Author: Abdur Rahim Sujon, University of Salford">

    <!--Bootstrap CSS link-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
          crossorigin="anonymous">
    <!-- Leaflet CSS header -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
    <!--Custom CSS links-->
    <link href="css/index.css" rel="stylesheet">
    <link href="css/login.css" rel="stylesheet">
    <link href="css/sightings.css" rel="stylesheet">
    <link rel="stylesheet" href="css/map.css">

    <title>petWatch</title>
</head>

<!-- Page container for layout styling -->
<div class="page-container">
    <body class="d-flex flex-column">
    <header>
        <nav class="navbar navbar-expand-xl navbar-dark fixed-top bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand mx-0 mx-lg-2 mx-xxl-5"
                   href="index.php">
                    <img src="images/logo.png" alt="pet watch logo"
                         class="logo">Pet Watch
                </a>

                <button class="navbar-toggler" type="button"
                        data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
                        aria-controls="navbarCollapse"
                        aria-expanded="false"
                        aria-label="Toggle navigation">
                    <img src="images/menu-icon.png" alt="menu icon">
                </button>

                <div class="collapse navbar-collapse"
                     id="navbarCollapse">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page"
                               href="index.php">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link"
                               href="viewSightings.php">View Sightings</a>
                        </li>

                        <!-- Show Add & Manage Pets if role is Owner -->
                        <?php if (
                                isset($_SESSION['loggedin'])
                                && $_SESSION['loggedin'] === true
                                && isset($_SESSION['role'])
                                && $_SESSION['role'] === 'Owner'
                        ): ?>
                            <li class="nav-item">
                                <a class="nav-link"
                                   href="mypet.php">Add & Manage Pets</a>
                            </li>
                        <?php endif; ?>

                        <li class="nav-item">
                            <a class="nav-link"
                               href="createSightings.php">Create & Edit Sightings</a>
                        </li>
                    </ul>

                    <!-- Header search form -->
                    <form class="w-100 w-lg-50 my-2 my-lg-0"
                          role="search"
                          action="viewSightings.php"
                          method="get"
                          style="max-width: 400px;">
                        <div class="input-group w-100">
                            <input type="search" name="search"
                                   class="form-control py-2 px-3"
                                   placeholder="Search by pet name, species type, etc."
                                   value="<?= htmlspecialchars($view->searchTerm ?? '') ?>"
                                   aria-label="Search">
                            <button class="btn btn-success px-4"
                                    type="submit">Search
                            </button>
                        </div>
                    </form>

                    <ul class="navbar-nav d-flex mb-2 mt-3 mt-lg-0 mb-lg-0">
                        <!-- If logged in, show user and logout -->
                        <?php if (
                                isset($_SESSION['loggedin'])
                                && $_SESSION['loggedin'] === true
                        ): ?>
                            <li class="nav-item mt-2">
                                <?php $role = $_SESSION['role'] ?? 'Unknown Role'; ?>
                                <p class="user-details text-light mb-2">
                                    <?= htmlspecialchars($_SESSION['username']) ?>
                                    is logged in
                                    <span class="badge bg-warning text-dark">
           (Role: <?= $role ?>)
          </span>
                                </p>
                            </li>
                            <li class="nav-item mx-0 mx-lg-2 my-lg-4 my-xl-2">
                                <a href="login.php?action=logout"
                                   class="btn btn-danger btn-large py-2">
                                    Logout
                                </a>
                            </li>
                        <?php else: ?>
                            <!-- If not logged in, show login -->
                            <li class="nav-item mx-0 mx-xl-5 pt-3 pt-xl-0">
                                <a href="login.php"
                                   class="btn btn-primary btn-large">Login</a>
                            </li>
                        <?php endif; ?>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="flex-shrink-0">

