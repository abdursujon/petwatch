CREATE TABLE users
(
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    email VARCHAR(255) NOT NULL UNIQUE,
    passward_hash VARCHAR(255) NOT NULL,
    role VARCHAR(50) DEFAULT 'User',
    check (role IN ('User', 'Owner'))
)

CREATE TABLE sightings
(
      id        INT AUTO_INCREMENT PRIMARY KEY,
      pet_id    INT NOT NULL,
      user_id   INT,
      comment   TEXT,
      latitude  DOUBLE,
      longitude DOUBLE,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (pet_id) REFERENCES pets(id),
      FOREIGN KEY (user_id) REFERENCES users(id)
)

CREATE TABLE pets
(
      id            INT AUTO_INCREMENT PRIMARY KEY,
      name          VARCHAR(255) NOT NULL,
      species       VARCHAR(100) NOT NULL,
      breed         VARCHAR(255),
      color         VARCHAR(100),
      photo_url     TEXT,
      status        VARCHAR(50) NOT NULL,
      description   TEXT,
      date_reported DATE NOT NULL,
      user_id       INT,
      CHECK (status IN ('lost', 'found')),
      FOREIGN KEY (user_id) REFERENCES users(id)
)

CREATE TABLE sightings
(
      id        INT AUTO_INCREMENT PRIMARY KEY,
      pet_id    INT NOT NULL,
      user_id   INT,
      comment   TEXT,
      latitude  DOUBLE,
      longitude DOUBLE,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (pet_id) REFERENCES pets(id),
      FOREIGN KEY (user_id) REFERENCES users(id)
)

How to bult fix db storage information such as photo name path
UPDATE pets SET photo_url = SUBSTRING(photo_url, 2) WHERE  photo_url LIKE '/%';

Tech Stack for T2
  1. JavaScript (vanilla/raw) — no jQuery allowed                               
  2. AJAX (using fetch or XMLHttpRequest) — for async server communication
  3. PHP (MVC pattern) — extending your Assignment 1                            
  4. JSON/XML — structured data exchange between client & server              
  5. Mapping API (e.g., Find the one that used in workshops) — interactive maps with markers & popups
  6. Geolocation API (It's a built-in browser feature) — browser-based user location
  7. MariaDB (MariaDB for higher marks)
  8. HTML/CSS — responsive UI
  9. OOP in JS & PHP — classes, design patterns
  10. Security techniques — CSRF tokens, input validation, SQL injection
  prevention, output encoding

  Key constraints:
  - No jQuery
  - No WebSockets
  - No MySQL (use MariaDB or SQLite)
  - Must be hosted on Poseidon server
  - Must handle 100s of records

  For the chosen feature (pick one):
  - Live Search — debounced AJAX search with filters

